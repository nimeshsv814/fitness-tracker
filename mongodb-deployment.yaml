apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: nimesh-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mongodb
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:7-jammy
        ports:
        - containerPort: 27017
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "130Mi"
            cpu: "200m"
        volumeMounts:
          - name: nimesh-data
            mountPath: /data/db
      volumes:
        - name: nimesh-data
          emptyDir:
            sizeLimit: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: nimesh-ns
spec:
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP
 
 